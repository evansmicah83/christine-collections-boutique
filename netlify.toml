[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  SECRETS_SCAN_OMIT_PATHS = ".env"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/server"
  status = 200
